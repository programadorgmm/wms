{% spaceless %}
    {% if is_expr_granted("hasAnyRole('ROLE_ADMIN','ROLE_STOCK_POSITION_UPDATE')") %}
        <a href="{{ path('stock_position_update', {'id': row.id}) }}" class="btn btn-default btn-xs"><i
                    class="glyphicon glyphicon-pencil"></i> {% trans %}Edit{% endtrans %}</a>
    {% endif %}
    {% if is_expr_granted("hasAnyRole('ROLE_ADMIN','ROLE_STOCK_POSITION_DELETE')") %}
        <a href="{{ path('stock_position_delete', {'id': row.id}) }}"
           onclick="javascript:return confirm('{% trans %}Are you sure?{% endtrans %}');" class="btn btn-danger btn-xs"><i
                    class="glyphicon glyphicon-trash"></i> {% trans %}Delete{% endtrans %}</a>
    {% endif %}
{% endspaceless %}
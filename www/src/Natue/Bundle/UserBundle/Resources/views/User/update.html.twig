{% extends '::base.html.twig' %}

{% block headline %}
    {% trans %}Update User{% endtrans %}
{% endblock headline %}

{% block content %}
    <form action="{{ path('user_user_update', {'id': user.id}) }}" method="post" {{ form_enctype(form) }}
          class="form-horizontal">
        {{ form_widget(form) }}


        <div class="form-group">
            <div class="col-sm-offset-3 col-sm-9">
                <button type="submit" class="btn btn-primary">{% trans %}Submit{% endtrans %}</button>
                {% if is_expr_granted("hasAnyRole('ROLE_SUPER_ADMIN','ROLE_USER_USER_DELETE')") %}
                    <a href="{{ path('user_user_delete', {'id': user.id}) }}"
                       onclick="javascript:return confirm('{% trans %}Are you sure?{% endtrans %}');"
                       class="btn btn-danger pull-right"><i class="glyphicon glyphicon-trash"></i> {% trans %}
                        Delete{% endtrans %}</a>
                {% endif %}
            </div>
        </div>
    </form>
{% endblock content %}